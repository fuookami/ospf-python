"""倒数运算符。

Reciprocal operator.
"""

from __future__ import annotations


def reciprocal_op(x: float) -> float:
    """计算倒数 (1/x)。

    Compute the reciprocal (1/x).

    Args:
        x: 输入值（非零）。/ Input value (non-zero).

    Returns:
        倒数值。/ Reciprocal value.
    """
    return 1.0 / x
